import threading
import os

def print_string():

    while True:
       os.system("sudo nmap -Pn 141.26.68.155")
      # os.system("ssh beni1@141.26.68.155 -p 80")

threads = []
for i in range(200):
    t = threading.Thread(target=print_string)
    threads.append(t)
    t.start()

for t in threads:
    t.join()
